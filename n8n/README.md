# n8n

Esta pasta foi reservada para futuros artefatos do n8n.

O arquivo `workflow-development.json` não foi gerado nesta estrutura inicial porque não é possível garantir compatibilidade importável sem confirmar a versão instalada do n8n, os tipos de credenciais disponíveis e os nós habilitados no ambiente.

Use `../docs/fluxo-conversa.md` para montar o workflow manualmente, nó por nó.
